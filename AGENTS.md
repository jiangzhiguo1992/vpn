# AGENTS.md

---

## 项目概述
> 参考文档 [`README.md`](README.md)

## Agent 交互协议

- 所有包/模块的根目录下都可能有 **AGENTS.md** 文件,所属包/模块**必须**严格参照这些文档的规范和建议。
- 每次会话都要携带当前文档或相关 AGENTS.md 的所有内容作为上下文,禁止因上下文缺失而导致的误解、错误、遗漏。
- 全程使用**简体中文**回答(技术术语、代码标识符、error 内容除外),包括思考(thinking)过程。
- 未经允许,不能擅自 commit 和 push。未经允许,不能动 git 暂存状态,新的修改不放在暂存里,区分开来。

### 核心规则：每次回复结束前必须提出后续问题

这是不可跳过的强制协议。**每一轮回复的最后一段**必须是一个与当前任务直接相关的后续问题或确认请求——保持对话连续性，禁止自行关闭话题。

具体要求：

1. **完成用户请求后** → 在回复末尾提出与当前上下文相关的后续问题(如发现的待决问题、可优化的方向、可扩展的功能、下一步方向确认等)
2. **涉及代码的生成/变动时** → 后续问题的**第一个选项必须**是对刚编写代码的深度排查/优化(如潜在问题、性能衰减/改进、过度设计/优化等),其余选项再覆盖其他方向
3. **可提供选项降低用户输入成本** → 如"接下来可以：1) …。 2) …。 3) …。 4) …。 5) …。 选哪个？"

### 不确定性澄清

当任务涉及以下**任何**不确定性时,Agent **必须**先向用户确认再行动,禁止猜测:

- 任何有取舍争议、困惑澄清、歧义解释、假设猜测、适度异议等的设计和改造
- 任何可能引入性能衰减、内存压力、过度设计、过度优化等的设计和改造

### 禁止行为

- **禁止使用终结性表达** ——"希望对你有帮助"、"如有问题随时提问"、"以上是全部修改"等一律禁止
- **禁止猜测用户意图** ——存在不确定性时先提问澄清,确认后再行动(参见[不确定性澄清](#不确定性澄清))
- **禁止盲目创建文件** ——创建新文件前必须先检查目标路径是否已存在;若已存在,直接修改或重写

---

## Agent 代码编写/分析/审查规范

核心逻辑:**先保证能用(正确性)→ 再保证好用(性能优化)→ 最后保证耐用(鲁棒性)**。

### 阶段路由

|任务类型|执行阶段|说明|
|-------|-------|------|
|简单任务|无|直接进行,不受阶段限制|
|需求分析|一 → 二|仅分析和设计,不编码|
|新增/扩展功能|一 → 六(全部)|完整流程|
|改造/优化功能|二 → 六|跳过需求分析|
|修复 Bug|三 → 六|跳过需求分析和架构设计|
|全面分析/审查|一 → 六(全部)|以规范为标准逐项比对|

---

### 第一阶段：需求分析与边界定义

动手之前先搞清楚"为什么"和"做什么"和"在哪做"：

- **需求参考**：客观理性的参考其他主流框架，都解决了什么需求，评估并借鉴
- **需求抉择**：与用户确认需求的具体内容、背景、目的，避免误解和过度设计
- **需求拆解**：将用户需求拆解为具体的技术任务清单，列出涉及的包和文件
- **职责归属**：确认每项变更属于哪个包/文件的职责边界（各文件头部要有记录）
- **需求确认**：涉及 [不确定性](#不确定性澄清) 时先向用户确认，禁止猜测

---

### 第二阶段：架构/方案设计

确定"怎么做"——技术方案需在编码前明确。

#### 2.1 方案设计流程

- **设计参考**：客观理性的参考其他主流框架的架构和方案设计，评估其优缺点和适用场景并借鉴
- **模式复用**：优先复用项目已有模式/功能/代码块，不造新轮子，新引入需有充分理由
- **单元拆分**：将复杂功能拆成可独立实现和验证的最小单元，避免一次性写大而全的代码
- **职责归属**：确认新代码/文件属于哪一层，遵循已有的分层结构或设计更好的分层结构
- **方案确认**：涉及 [不确定性](#不确定性澄清) 时先向用户确认，禁止猜测

#### 2.2 设计架构

以 Go 的设计理念落实，积极并合理的使用以下设计：

- 如单一职责、开放封闭、里氏替换、依赖倒置、接口隔离、高内聚+低耦合、可扩展性、可维护性、可监测性、等

#### 2.3 设计范式

需根据场景选择适合 Go 的范式，积极并合理的使用以下设计：

- 如面向接口、面向组合、面向对象、面向过程、面向函数、面向数据、面向事件、面向领域、面向并发、等

#### 2.4 扩展性 vs 性能取舍

- 冷路径优先扩展性；热路径优先性能。

- 所有可能造成性能衰减和内存压力的设计决策都必须**必须向用户确认**，不可自行决定

---

### 第三阶段：代码编写/分析/审查（核心环节）

编码原则：先跑通再完善和优化 → 避免过度设计/优化 → 边写边注释。

#### 3.1 文件内声明顺序

```text
package 声明
文件头块注释（用途 → 设计决策 → 职责边界 → 使用示例）
import（标准库 / 内部包 / 第三方）
const
var
type
New... 构造函数
公开方法（组间 // ===== 分组名 =====）
私有方法
辅助函数
```

#### 3.2 命名规范

- 包/文件/类型/方法/常量/变量命名遵循 Go 社区约定，语义清晰不混淆
- 仅包外需要的才大写导出，内部实现一律小写
- 可参考其他主流档案和框架的命名规范，保持一致性和可读性

#### 3.3 编码规范

逐行分析生成/改动的代码，确保每行代码都符合以下规范：

- 禁止使代码冗余或臃肿，禁止重复实现或造轮子，越精简越好
- 删除因改动而变得无用的导入/变量/函数/文件/等，禁止遗留无用、孤儿代码
- 禁止随意添加不符合职责边界的代码/文件，需分配/拆分在正确的层/模块中
- 跳过无关紧要、收益细微的优化，避免强行优化和过度优化
- 匹配现有风格，禁止风格不一致的代码，即使你更倾向于不同的写法
- 以 `go.mod` 中声明的Go版本为准，积极主动使用新版本的特性和标准库函数

#### 3.4 性能优化

对生成/改动的代码进行逐行分析(可参考其他主流框架的性能优化)：

- **速度**：算法复杂度、内联友好性、CPU缓存、分支预测、装箱/拆箱、寻址效率、过度判断、等
- **内存**：空间复杂度、分配频率、逃逸分析（堆vs栈）、GC压力、内存浪费、字段/内存对齐、切片预分配、内存复用、等
- **I/O**：读写效率、缓冲策略、吞吐量、异步I/O、压缩、等
- **并发**：线程安全、锁粒度、死锁/活锁、原子操作、FalseSharing、等
- **转换**：接口虚调用、类型断言开销、数据类型溢出、等

#### 3.5 鲁棒性增强

- 逐行排查**潜在问题**如：功能缺陷、逻辑错误、运行时错误、溢出保护、数据安全、防御性编程、等代码健壮性问题。

#### 3.6 错误处理与日志

- **error** 不返回的 error 必须打印（含栈信息 + 上下文：函数、参数、值）
- **日志格式** `包名.结构体名.函数名`，含关键变量值
- **防御性编程** 只在debug模式下检测并断言(Assert)，禁止运行时标志

#### 3.7 注释规范

- 中文为主，通俗易懂；化繁为简，禁止多处重复注释
- 文件顶部块注释 + ASCII 图：用途 → 设计决策 → 职责边界 → 使用示例
- 方法必须有注释，且须覆盖：功能描述 → 调用说明 → 参数说明 → 返回值说明
- `unsafe` 标注安全前提（❌ 和 ⚠️ 标风险）；算法注释步骤和复杂度；复杂理论需图解
- 代码变更时同步更新旧注释，禁止遗留过时注释，禁止遗留如"v1"、"修复说明"等会腐烂的注释

---

### 第四阶段：测试验证

基本要求：`go build ./...` 无错误 + `go vet ./...` 无问题。用户要求编写测试时或已有测试实例的代码变更时额外遵循：

- **覆盖率**：所有导出和非导出方法，多维度（正常/边界/异常），断言验证返回值 + 状态 + 副作用
- **基准测试**：`testing.B` + `b.ReportAllocs()`，测试各指标结果写入测试文件头注释(注意结果排版，要求为表格形式的关系型数据格式展示)
- **竞态检测**：涉及并发的代码必须通过 `go test -race ./...`，CI 必跑
- **测试环境**：单元测试在debug模式下运行，基准和竞态等性能测试在release模式下运行，模拟正式生产环境
- **测试规模**：基准测试的规模必须要有百千万级(递增)，竞态测试的规模要有百万级(递增)，以暴露并发问题和性能趋势
- **代码组织**：按功能分组（`// ===== 分组 =====`），命名 `Test<类型>_<方法>_<场景>`，表驱动优先
- **发现问题**：立即修复并补回归用例，并遵守第三阶段的规范

---

### 第五阶段：文档编写

所有的代码变更都尽量伴随文档更新，确保用户能理解"为什么"和"怎么用"。

#### 5.1 源码文件头文档（文件的 README）

- 各实现文件头注释遵循 [3.7 注释规范](#37-注释规范)；职责变化时更新"职责边界"注释

#### 5.2 版本迭代总结文档（所有代码/功能/文件的分析或审查的 README）

- 存放路径：`docs/<模块>/summary/<功能>/v<N>.md`（如 `docs/ecs/summary/store/v2.md` 版本号叠加）

- 文档内容覆盖多个维度（潜在风险、可扩展功能、功能冗余、过度设计、可优化点、过度优化、等），需包含完整的分析推导过程、备选方案对比、影响范围评估

- 另外每条建议必须有**具体可操作**原因和指南，并标注优先级（必要/建议/可选）。不属于当前模块职责的建议需注明"跨模块"，由用户决定是否采纳

#### 5.3 模块级介绍文档（模块的 README）

- 章节顺序：目录 → 快速开始 → 主流框架对比(需客观理性) → 架构设计（UML/接口图）→ 构建流程 → 文件结构与职责 → 功能介绍（导出类型→属性→函数）→ 完整示例 → 单元测试 → FAQ

- 原则：仅面向外部调用者（不暴露内部实现）| 注明适用/禁用场景 | 框架对比客观（需基准数据）| 格式严谨一致 | 可参考 `docs/` | 自给自足无需翻源码

- 使用 **Mermaid** 绘制 UML/接口图/流程图/等各种格式化图表，合理应用丰富的关键字/标签来构建，并注意和文字描述的对应关系，保证图文一致性和可读性

---

### 第六阶段：最终交付

**重点注意**：最终交付前的收尾工作——全面检索代码，一定要通过**多轮检查**(按路由阶段)确认无遗漏，并保证无其他问题。

#### 6.1 全量排查

- 逐**文件**排查(重点)，逐**行**排查(重点)，按第二～五阶段逐**维度**比对，汇总清单标注严重程度（必要/建议/可选）

#### 6.2 改造约束

- 每项改造必须**同时满足**：① 在职责范围内 ② 非过度设计 ③ 非过度优化 ④ 无性能衰减 ⑤ 附注释标注改造原因

#### 6.3 交付确认

- `go build ./...` + `go build -tags release ./...` + `go vet ./...` 无回归 → 向用户总结变更（改了什么、为什么改、影响范围）
