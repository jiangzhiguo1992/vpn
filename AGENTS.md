# AGENTS.md

---

## 项目概述
> 参考文档 [`README.md`](README.md)
> 本仓库是一套自建 VPN(代理)方案:sing-box 服务端 + 多客户端配置生成,从 0 到 1 的完整闭环。

## Agent 交互协议

- 所有包/模块的根目录下都可能有 **AGENTS.md** 文件,所属包/模块**必须**严格参照这些文档的规范和建议。
- 每次会话都要携带当前文档或相关 AGENTS.md 的所有内容作为上下文,禁止因上下文缺失而导致的误解、错误、遗漏。
- 全程使用**简体中文**回答(技术术语、代码标识符、error 内容除外),包括思考(thinking)过程。
- 未经允许,不能擅自 commit 和 push。未经允许,不能动 git 暂存状态,新的修改不放在暂存里,区分开来。

### 不确定性澄清

当任务涉及以下**任何**不确定性时,Agent **必须**先向用户确认再行动,禁止猜测:

- 任何有取舍争议、困惑澄清、歧义解释、假设猜测、适度异议等的设计和改造
- 任何可能引入性能衰减、内存压力、过度设计、过度优化等的设计和改造

### 禁止行为

- **禁止使用终结性表达**("希望对你有帮助"、"如有问题随时提问"、"以上是全部修改"等一律禁止)
- **禁止猜测用户意图**,存在不确定性时先提问澄清,确认后再行动(参见[不确定性澄清](#不确定性澄清))
- **禁止盲目创建文件**,创建新文件前必须先检查目标路径是否已存在;若已存在,直接修改或重写
- **禁止在推理与回复中输出无意义的装饰符号**;表达用简洁中文与常规标点

---

## Agent 代码编写/分析/审查规范

核心逻辑:**先保证能用(正确性)→ 再保证好用(性能优化)→ 最后保证耐用(鲁棒性)**。

### 阶段路由

| 任务类型 | 执行阶段 | 说明 |
|---------|---------|------|
| 简单任务 | 无 | 直接进行,不受阶段限制 |
| 需求分析 | 一 → 二 | 仅分析和设计,不编码 |
| 新增/扩展功能 | 一 → 六(全部) | 完整流程 |
| 改造/优化功能 | 二 → 六 | 跳过需求分析 |
| 修复 Bug | 三 → 六 | 跳过需求分析和架构设计 |
| 全面分析/审查 | 一 → 六(全部) | 以规范为标准逐项比对 |

### 第一阶段:需求分析与边界定义

动手之前先搞清楚"为什么"和"做什么"和"在哪做":

- **需求参考**:客观理性的参考其他主流框架,评估并借鉴
- **需求抉择**:与用户确认需求的具体内容、背景、目的,避免误解和过度设计
- **需求拆解**:将用户需求拆解为具体的技术任务清单,列出涉及的包和文件
- **职责归属**:确认每项变更属于哪个包/文件的职责边界(各文件头部要有记录)
- **需求确认**:涉及[不确定性](#不确定性澄清)时先向用户确认,禁止猜测

### 第二阶段:架构/方案设计

- 以 Go 的设计理念落实:单一职责、开放封闭、高内聚+低耦合、可扩展性、可维护性
- 冷路径优先扩展性,热路径优先性能
- 所有可能造成性能衰减和内存压力的设计决策都必须向用户确认

### 第三阶段:代码编写/分析/审查(核心环节)

编码原则:先跑通再完善和优化,避免过度设计/优化,边写边注释。

#### 3.1 文件内声明顺序

```text
package 声明
文件头块注释(用途 → 设计决策 → 职责边界 → 使用示例)
import(标准库 / 内部包 / 第三方)
const
var
type
New... 构造函数
公开方法(组间 // ===== 分组 ===== 分隔)
私有方法
辅助函数
```

#### 3.2 命名规范

- 包/文件/类型/方法/常量/变量命名遵循 Go 社区约定,语义清晰不混淆
- 仅包外需要的才大写导出,内部实现一律小写
- 保持与项目既有命名风格一致

#### 3.3 编码规范

- 禁止使代码冗余或臃肿,禁止重复实现或造轮子,越精简越好
- 删除因改动而变得无用的导入/变量/函数/文件,禁止遗留无用、孤儿代码
- 禁止随意添加不符合职责边界的代码/文件,需分配/拆分在正确的层/模块中
- 跳过无关紧要、收益细微的优化,避免强行优化和过度优化
- 匹配现有风格,禁止风格不一致的代码
- 以 `go.mod` 中声明的 Go 版本为准,积极主动使用新版本的特性和标准库函数

#### 3.4 鲁棒性增强

- 逐行排查潜在问题:功能缺陷、逻辑错误、运行时错误、溢出保护、数据安全、防御性编程等

#### 3.5 错误处理与日志

- error 不返回的 error 必须打印(含栈信息 + 上下文:函数、参数、值)
- 防御性编程只在 debug 模式下检测并断言,禁止运行时标志

#### 3.6 注释规范

- 中文为主,通俗易懂;化繁为简,禁止多处重复注释
- 文件顶部块注释 + ASCII 图:用途 → 设计决策 → 职责边界 → 使用示例
- 方法必须有注释,且须覆盖:功能描述 → 调用说明 → 参数说明 → 返回值说明
- 代码变更时同步更新旧注释,禁止遗留过时注释

### 第四阶段:测试验证

基本要求:`go build ./...` 无错误 + `go vet ./...` 无问题。另有:

- **单测**:所有导出方法多维度(正常/边界/异常)覆盖,表驱动优先,命名 `Test<类型>_<方法>_<场景>`
- **产物一致性**:生成类代码必须有断言测试,验证产物关键字段与输入一致(防凭据漂移)
- 发现问题立即修复并补回归用例,遵守第三阶段规范

### 第五阶段:文档编写

所有代码变更都尽量伴随文档更新,确保用户能理解"为什么"和"怎么用"。

- **总 README**:项目定位、快速开始、目录结构、文档地图(关联 docs/ 下各子文档)
- **docs/ 子文档**:面向具体环节(部署、客户端对接、平台矩阵、架构、FAQ),格式严谨,使用 Mermaid 绘制图表时注意与文字描述对应
- 文档与代码命令、端口、字段必须一致,禁止漂移

### 第六阶段:最终交付

- 逐文件、逐行排查,按第二到五阶段逐维度比对,汇总清单标注严重程度(必要/建议/可选)
- 每项改造必须同时满足:在职责范围内、非过度设计、非过度优化、无性能衰减、附注释标注改造原因
- `go build ./...` + `go vet ./...` + `go test ./...` 无回归,产物命令按 README 走通后再向用户总结变更(改了什么、为什么改、影响范围)
