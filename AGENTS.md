# AGENTS.md

---

## 项目概述
> 参考文档 [`README.md`](README.md)

## Agent 交互协议

- 所有包/模块的根目录下都可能有 **AGENTS.md** 文件,所属包/模块**必须**严格参照这些文档的规范和建议。
- 每次会话都要携带当前文档或相关 AGENTS.md 的所有内容作为上下文,禁止因上下文缺失而导致的误解、错误、遗漏。
- 全程使用**简体中文**回答(技术术语、代码标识符、error 内容除外),包括思考(thinking)过程。
- 未经允许,不能擅自 commit 和 push。未经允许,不能动 git 暂存状态,新的修改不放在暂存里,区分开来。

### 核心规则：每次回复结束前必须提出后续问题

这是不可跳过的强制协议。**每一轮回复的最后一段**必须是一个与当前任务直接相关的后续问题或确认请求——保持对话连续性，禁止自行关闭话题。

具体要求:

1. **完成用户请求后** → 在回复末尾提出与当前上下文相关的后续问题(如发现的待决问题、可优化的方向、可扩展的功能、下一步方向确认等)
2. **涉及代码的生成/变动时** → 后续问题的**第一个选项必须**是对刚编写代码的深度排查/优化(如潜在问题、性能衰减/改进、过度设计/优化等),其余选项再覆盖其他方向
3. **可提供选项降低用户输入成本** → 如"接下来可以：1) …。 2) …。 3) …。 4) …。 5) …。 选哪个？"

### 不确定性澄清

当任务涉及以下**任何**不确定性时,Agent **必须**先向用户确认再行动,禁止猜测:

- 任何有取舍争议、困惑澄清、歧义解释、假设猜测、适度异议等的设计和改造
- 任何可能引入性能衰减、内存压力、过度设计、过度优化等的设计和改造

### 禁止行为

- **禁止使用终结性表达** ——"希望对你有帮助"、"如有问题随时提问"、"以上是全部修改"等一律禁止
- **禁止猜测用户意图** ——存在不确定性时先提问澄清,确认后再行动(参见[不确定性澄清](#不确定性澄清))
- **禁止盲目创建文件** ——创建新文件前必须先检查目标路径是否已存在;若已存在,直接修改或重写

---

## 最终交付

**重点注意**:最终交付前的收尾工作——全面检索代码,一定要通过**逐文件逐行**、**多轮检查**确认无遗漏,确保没有引入新问题。

### 改造约束

- 每项改造必须**同时满足**:① 在职责范围内 ② 非过度设计 ③ 非过度优化 ④ 无性能衰减 ⑤ 附注释标注改造原因

### 交付确认

- `go build ./...` + `go build -tags release ./...` + `go vet ./...` 无回归 → 向用户总结变更（改了什么、为什么改、影响范围）
